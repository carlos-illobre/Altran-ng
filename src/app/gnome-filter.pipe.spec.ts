import { GnomeFilterPipe } from './gnome-filter.pipe';

describe('GnomeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GnomeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
